---
phase: 06-content-migration-and-cleanup
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - frontend/src/lib/utils.ts
  - frontend/src/lib/error-handler.ts
  - frontend/src/data/strapi-loaders.ts
  - frontend/src/data/strapi-data-api.ts
  - frontend/src/components/ui/strapi-image.tsx
  - frontend/src/components/ui/strapi-video.tsx
  - frontend/scripts/optimize-avif.ts
  - frontend/scripts/upload-landing-images.ts
  - frontend/scripts/apply-optimization.ts
  - frontend/next.config.ts
  - frontend/package.json
  - frontend/package-lock.json
autonomous: true
requirements:
  - CLEAN-01
  - CLEAN-02
  - CLEAN-03
  - CLEAN-04
  - CLEAN-05

must_haves:
  truths:
    - "No file in frontend/src/ imports from strapi-loaders, strapi-data-api, strapi-image, strapi-video, or error-handler"
    - "The backend/ directory does not exist"
    - "frontend/src/lib/utils.ts exports only cn() — no getStrapiURL or fetchStrapi"
    - "frontend/next.config.ts remotePatterns contains only cdn.sanity.io — no strapiapp.com, no localhost:1337, no vercel-storage"
    - "frontend/package.json does not contain qs, @types/qs, or @vercel/blob"
    - "No file in frontend/src/ references STRAPI, getStrapiURL, or fetchStrapi"
    - "npm install and npm run build both succeed with zero errors"
  artifacts:
    - path: "frontend/src/lib/utils.ts"
      provides: "cn() utility only"
      contains: "export function cn"
    - path: "frontend/next.config.ts"
      provides: "Next.js config with Sanity CDN only"
      contains: "cdn.sanity.io"
  key_links:
    - from: "frontend/src/lib/utils.ts"
      to: "13+ component files"
      via: "cn() export"
      pattern: "export function cn"
---

<objective>
Remove all Strapi and Vercel Blob artifacts from the codebase: delete dead files, clean up utils.ts, remove Strapi/Blob remote patterns from next.config.ts, uninstall unused packages, and delete the backend/ directory.

Purpose: After migration is verified (Plan 01), this cleanup ensures the codebase has zero Strapi remnants — the site runs purely on Sanity.
Output: Clean codebase with no Strapi references, passing build.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-content-migration-and-cleanup/06-CONTEXT.md
@.planning/phases/06-content-migration-and-cleanup/06-01-SUMMARY.md

<interfaces>
<!-- utils.ts current state — must preserve cn() and its imports -->
From frontend/src/lib/utils.ts:
```typescript
import { clsx, type ClassValue } from "clsx"
import { twMerge } from "tailwind-merge"

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs))
}
// REMOVE everything below this line (getStrapiURL, fetchStrapi)
```

<!-- next.config.ts current remotePatterns — keep only cdn.sanity.io -->
From frontend/next.config.ts:
```typescript
remotePatterns: [
  { protocol: "https", hostname: "cdn.sanity.io", pathname: "/images/**" },
  // REMOVE: localhost:1337, strapiapp.com, vercel-storage.com entries
]
```

<!-- package.json packages to remove -->
dependencies: "qs" (^6.15.0), "@types/qs" (^6.15.0)
devDependencies: "@vercel/blob" (^2.3.1)

<!-- package.json scripts to remove -->
"migrate:blob", "optimize:test", "optimize:apply", "optimize:cleanup"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete dead Strapi/Blob files and backend directory</name>
  <files>
    frontend/src/data/strapi-loaders.ts
    frontend/src/data/strapi-data-api.ts
    frontend/src/components/ui/strapi-image.tsx
    frontend/src/components/ui/strapi-video.tsx
    frontend/src/lib/error-handler.ts
    frontend/scripts/optimize-avif.ts
    frontend/scripts/upload-landing-images.ts
    frontend/scripts/apply-optimization.ts
    backend/
  </files>
  <read_first>
    frontend/src/lib/error-handler.ts
    frontend/src/data/strapi-loaders.ts
    frontend/src/data/strapi-data-api.ts
    frontend/src/components/ui/strapi-image.tsx
    frontend/src/components/ui/strapi-video.tsx
  </read_first>
  <action>
Delete the following 8 files (per D-05). These files have zero importers in `frontend/src/` — verified by grep in planning:

```bash
rm frontend/src/data/strapi-loaders.ts
rm frontend/src/data/strapi-data-api.ts
rm frontend/src/components/ui/strapi-image.tsx
rm frontend/src/components/ui/strapi-video.tsx
rm frontend/src/lib/error-handler.ts
rm frontend/scripts/optimize-avif.ts
rm frontend/scripts/upload-landing-images.ts
rm frontend/scripts/apply-optimization.ts
```

Delete the entire backend directory (per D-05, CLEAN-01):
```bash
rm -rf backend/
```

KEEP `frontend/scripts/vercel-install.mjs` — it handles Motion dev token injection, unrelated to Strapi.
KEEP `frontend/scripts/migrate-strapi-to-sanity.ts` — created in Plan 01, still useful for reference/re-runs.

Before deleting, verify no active imports exist:
```bash
grep -r "strapi-loaders\|strapi-data-api\|strapi-image\|strapi-video\|error-handler" frontend/src/ --include="*.ts" --include="*.tsx" | grep -v "\.md"
```
This should return empty. If any imports are found, do NOT delete those files — instead log the unexpected import for investigation.
  </action>
  <verify>
    <automated>cd /Users/dardan/Documents/lash-her && ! test -f frontend/src/data/strapi-loaders.ts && ! test -f frontend/src/data/strapi-data-api.ts && ! test -f frontend/src/components/ui/strapi-image.tsx && ! test -f frontend/src/components/ui/strapi-video.tsx && ! test -f frontend/src/lib/error-handler.ts && ! test -f frontend/scripts/optimize-avif.ts && ! test -f frontend/scripts/upload-landing-images.ts && ! test -f frontend/scripts/apply-optimization.ts && ! test -d backend/ && echo "All dead files deleted"</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/data/strapi-loaders.ts does not exist
    - frontend/src/data/strapi-data-api.ts does not exist
    - frontend/src/components/ui/strapi-image.tsx does not exist
    - frontend/src/components/ui/strapi-video.tsx does not exist
    - frontend/src/lib/error-handler.ts does not exist
    - frontend/scripts/optimize-avif.ts does not exist
    - frontend/scripts/upload-landing-images.ts does not exist
    - frontend/scripts/apply-optimization.ts does not exist
    - backend/ directory does not exist
    - frontend/scripts/vercel-install.mjs still exists (preserved)
    - frontend/scripts/migrate-strapi-to-sanity.ts still exists (preserved)
  </acceptance_criteria>
  <done>All 8 dead Strapi/Blob files deleted. Backend directory deleted. vercel-install.mjs and migrate-strapi-to-sanity.ts preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Clean utils.ts, next.config.ts, uninstall packages, remove Strapi scripts</name>
  <files>
    frontend/src/lib/utils.ts
    frontend/next.config.ts
    frontend/package.json
    frontend/package-lock.json
  </files>
  <read_first>
    frontend/src/lib/utils.ts
    frontend/next.config.ts
    frontend/package.json
  </read_first>
  <action>
**1. Edit `frontend/src/lib/utils.ts` (per CLEAN-03, D-05):**

Remove `getStrapiURL()` function (lines 8-10) and `fetchStrapi()` function (lines 12-54). Keep only `cn()` and its imports. The final file should be exactly:

```typescript
import { clsx, type ClassValue } from "clsx"
import { twMerge } from "tailwind-merge"

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs))
}
```

**2. Edit `frontend/next.config.ts` (per D-06, D-07, CLEAN-04):**

Remove these 3 remote pattern entries from the `images.remotePatterns` array:
- `{ protocol: "http", hostname: "localhost", port: "1337", pathname: "/uploads/**/*" }` (Strapi local dev)
- `{ protocol: "https", hostname: "cheerful-prosperity-b55f073006.media.strapiapp.com", pathname: "/**" }` (Strapi Cloud)
- `{ protocol: "https", hostname: "*.public.blob.vercel-storage.com", pathname: "/**" }` (Vercel Blob)

Also remove `dangerouslyAllowLocalIP: true` — it was only needed for local Strapi dev.

The final `images` config should be:
```typescript
images: {
  remotePatterns: [
    {
      protocol: "https",
      hostname: "cdn.sanity.io",
      pathname: "/images/**",
    },
  ],
},
```

**3. Remove Strapi npm scripts from `frontend/package.json`:**

Delete these 4 script entries:
- `"migrate:blob": "tsx scripts/upload-landing-images.ts"`
- `"optimize:test": "tsx scripts/optimize-avif.ts"`
- `"optimize:apply": "tsx scripts/apply-optimization.ts"`
- `"optimize:cleanup": "rm -rf .optimization-test-output"`

Keep the `"migrate"` script added in Plan 01.

**4. Uninstall unused packages (per CLEAN-02, CLEAN-04):**

```bash
cd frontend
npm uninstall qs @types/qs
npm uninstall --save-dev @vercel/blob
```

This removes:
- `qs` (^6.15.0) from dependencies — only used by deleted strapi-loaders.ts
- `@types/qs` (^6.15.0) from dependencies — type defs for qs
- `@vercel/blob` (^2.3.1) from devDependencies — only used by deleted optimization scripts

**5. Verify the build passes:**

```bash
cd frontend && npm run build
```

If build fails, inspect errors — they should NOT be related to this cleanup since no active code imports from the deleted files.
  </action>
  <verify>
    <automated>cd /Users/dardan/Documents/lash-her/frontend && ! grep -q "getStrapiURL\|fetchStrapi" src/lib/utils.ts && ! grep -q "strapiapp\|localhost.*1337\|vercel-storage\|dangerouslyAllowLocalIP" next.config.ts && ! grep -q '"qs"\|"@types/qs"\|"@vercel/blob"' package.json && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/lib/utils.ts contains `export function cn` (preserved)
    - frontend/src/lib/utils.ts does NOT contain `getStrapiURL`
    - frontend/src/lib/utils.ts does NOT contain `fetchStrapi`
    - frontend/src/lib/utils.ts is exactly 6 lines (imports + cn function)
    - frontend/next.config.ts contains `cdn.sanity.io`
    - frontend/next.config.ts does NOT contain `strapiapp.com`
    - frontend/next.config.ts does NOT contain `localhost`
    - frontend/next.config.ts does NOT contain `vercel-storage`
    - frontend/next.config.ts does NOT contain `dangerouslyAllowLocalIP`
    - frontend/package.json does NOT contain `"qs"`
    - frontend/package.json does NOT contain `"@types/qs"`
    - frontend/package.json does NOT contain `"@vercel/blob"`
    - frontend/package.json does NOT contain `"migrate:blob"`
    - frontend/package.json does NOT contain `"optimize:test"`
    - frontend/package.json does NOT contain `"optimize:apply"`
    - frontend/package.json does NOT contain `"optimize:cleanup"`
    - `cd frontend && npm run build` exits with code 0
    - `grep -r "strapi" frontend/src/ --include="*.ts" --include="*.tsx" | grep -v "BLOCK_RENDERER.md"` returns no results (or only the migrate script which is in scripts/, not src/)
  </acceptance_criteria>
  <done>utils.ts cleaned to cn() only. next.config.ts has only Sanity CDN remote pattern. qs, @types/qs, @vercel/blob packages uninstalled. Strapi-related npm scripts removed. Build passes with zero errors. No Strapi references remain in frontend/src/.</done>
</task>

</tasks>

<verification>
- `grep -r "strapi\|STRAPI\|Strapi" frontend/src/ --include="*.ts" --include="*.tsx"` returns empty (excluding .md files)
- `! test -d backend/` passes
- `cd frontend && npm run build` exits 0
- `cd frontend && npm test` passes (full Playwright suite)
</verification>

<success_criteria>
- Zero Strapi references in frontend/src/ TypeScript files
- backend/ directory deleted
- utils.ts exports only cn()
- next.config.ts has only cdn.sanity.io remote pattern
- qs, @types/qs, @vercel/blob removed from package.json
- npm build succeeds
- All existing Playwright tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-content-migration-and-cleanup/06-02-SUMMARY.md`
</output>
