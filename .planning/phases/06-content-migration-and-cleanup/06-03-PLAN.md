---
phase: 06-content-migration-and-cleanup
plan: 03
type: execute
wave: 3
depends_on: ["06-02"]
files_modified:
  - frontend/tests/training-programs.spec.ts
autonomous: false
requirements:
  - MIG-03
  - MIG-06
  - CLEAN-05

must_haves:
  truths:
    - "All 5 pages render correct content from Sanity — homepage, contact, gallery, training, training programs"
    - "All Playwright E2E tests pass across all browsers including training-programs rich text tests"
    - "No Strapi references exist anywhere in the codebase (excluding planning docs and git history)"
    - "The site is production-ready with zero regression"
  artifacts:
    - path: "frontend/tests/training-programs.spec.ts"
      provides: "E2E tests for training program detail pages with rich text rendering verification"
      min_lines: 40
  key_links: []
---

<objective>
Create E2E tests for training program detail pages to verify MIG-03 (rich text rendering), run the full Playwright test suite, do a comprehensive codebase grep for any remaining Strapi/Blob references, and have the user visually confirm all pages render correctly with migrated Sanity content.

Purpose: Ensures zero regression and verifies rich text conversion (Strapi Blocks to Portable Text) renders correctly on training program pages. This is the final quality gate for Phase 6.
Output: `frontend/tests/training-programs.spec.ts` + confirmed production-ready state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-content-migration-and-cleanup/06-01-SUMMARY.md
@.planning/phases/06-content-migration-and-cleanup/06-02-SUMMARY.md

<interfaces>
<!-- Training program detail page renders blocks via BlockRenderer -->
From frontend/src/app/(site)/training-programs/[slug]/page.tsx:
```typescript
import { BlockRenderer } from "@/components/custom/layouts/block-renderer";
// Loads data via loaders.getTrainingProgramBySlug(slug)
// Renders: <BlockRenderer blocks={data.blocks} />
```

<!-- Rich text appears in infoSection blocks (info field is Portable Text) -->
<!-- Portable Text renders: paragraphs, h2, h3, bullet lists, number lists, bold, italic, links -->

<!-- Existing training.spec.ts covers /training page and basic program detail navigation -->
<!-- but does NOT verify rich text content rendering (no checks for paragraphs, lists, headings) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create training-programs E2E test for rich text rendering</name>
  <files>frontend/tests/training-programs.spec.ts</files>
  <read_first>
    frontend/tests/training.spec.ts
    frontend/tests/utils/test-helpers.ts
    frontend/src/app/(site)/training-programs/[slug]/page.tsx
    frontend/src/components/custom/layouts/block-renderer.tsx
  </read_first>
  <action>
Create `frontend/tests/training-programs.spec.ts` to verify MIG-03 (rich text conversion from Strapi Blocks to Portable Text renders correctly on training program detail pages).

The test should:

1. Navigate to `/training` to discover program links, then visit a training program detail page (`/training-programs/{slug}`)
2. Verify the page loads without JS errors
3. Verify rich text content renders — check that the page contains:
   - At least one paragraph of text (a `p` element with visible text content inside `main`)
   - At least one heading (h2 or h3) inside `main`
4. Verify the page has proper content structure (main is not empty, has meaningful text)
5. Verify no broken images on the page (all `img` elements have non-empty `src` and load successfully)

Pattern the test after existing specs in `frontend/tests/` (use `setupApiMocks` if present, use `page.waitForLoadState('networkidle')`, use semantic selectors where possible).

Do NOT hardcode specific training program content — the test should work with any migrated content. The key verification is that rich text elements (paragraphs, headings) actually render, confirming Portable Text conversion succeeded.

If no training program links are found on `/training`, fall back to a direct navigation to `/training-programs/classic-lash-training` (known slug from existing test patterns).
  </action>
  <verify>
    <automated>cd /Users/dardan/Documents/lash-her/frontend && npx playwright test tests/training-programs.spec.ts --project=chromium 2>&1 | tail -10</automated>
  </verify>
  <done>training-programs.spec.ts exists, tests pass on Chromium, and the tests verify that rich text content (paragraphs, headings) renders on training program detail pages.</done>
</task>

<task type="auto">
  <name>Task 2: Run comprehensive verification suite</name>
  <files></files>
  <read_first>
    frontend/package.json
    frontend/next.config.ts
    frontend/src/lib/utils.ts
  </read_first>
  <action>
Run the following verification checks in sequence:

**1. Full Playwright E2E test suite (including new training-programs tests):**
```bash
cd frontend && npm test
```
All tests must pass across Chromium, Firefox, and WebKit.

**2. Codebase-wide Strapi reference scan:**
```bash
grep -r "strapi\|STRAPI\|Strapi\|strapiapp" frontend/src/ --include="*.ts" --include="*.tsx"
```
Must return zero results. If any results found, they are bugs from Plan 02 that need fixing.

**3. Codebase-wide Vercel Blob reference scan:**
```bash
grep -r "vercel-storage\|BLOB_READ_WRITE\|@vercel/blob" frontend/src/ --include="*.ts" --include="*.tsx"
grep -r "vercel-storage\|BLOB_READ_WRITE\|@vercel/blob" frontend/next.config.ts frontend/package.json
```
Must return zero results.

**4. Verify backend directory is gone:**
```bash
test -d backend && echo "FAIL: backend/ still exists" || echo "OK: backend/ deleted"
```

**5. Verify build passes:**
```bash
cd frontend && npm run build
```

**6. Check no orphaned imports remain:**
```bash
grep -r "from.*strapi\|from.*error-handler" frontend/src/ --include="*.ts" --include="*.tsx"
```
Must return zero results.

**7. Verify env var references are clean:**
```bash
grep -r "NEXT_PUBLIC_STRAPI\|STRAPI_BASE_URL\|STRAPI_API_TOKEN" frontend/src/ --include="*.ts" --include="*.tsx"
```
Must return zero results (migration script is in scripts/, not src/).

If any check fails, fix the issue before proceeding to the checkpoint.
  </action>
  <verify>
    <automated>cd /Users/dardan/Documents/lash-her/frontend && npm test 2>&1 | tail -5 && echo "---" && ! grep -rq "strapi\|STRAPI" src/ --include="*.ts" --include="*.tsx" && echo "No Strapi refs in src/"</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npm test` exits with code 0 (all browsers pass)
    - `grep -r "strapi\|STRAPI" frontend/src/ --include="*.ts" --include="*.tsx"` returns no results
    - `grep -r "vercel-storage\|@vercel/blob" frontend/ --include="*.ts" --include="*.tsx" --include="*.json" --include="next.config.ts"` returns no results (excluding node_modules)
    - `! test -d backend/` passes
    - `cd frontend && npm run build` exits with code 0
  </acceptance_criteria>
  <done>All Playwright tests pass (including new training-programs rich text tests). Zero Strapi/Blob references in codebase. Build succeeds. Backend directory deleted.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of migrated content</name>
  <files></files>
  <action>
User visually verifies that all pages render correctly with migrated Sanity content. This cannot be automated because it requires human judgment to confirm visual parity with the current production site.

Additionally, user should remove Strapi env vars from Vercel dashboard (per CLEAN-05):
- Remove: NEXT_PUBLIC_STRAPI_URL, NEXT_PUBLIC_STRAPI_API_TOKEN, BLOB_READ_WRITE_TOKEN
- Verify present: NEXT_PUBLIC_SANITY_PROJECT_ID, NEXT_PUBLIC_SANITY_DATASET, SANITY_WRITE_TOKEN, SANITY_WEBHOOK_SECRET

Steps to verify:
1. Visit http://localhost:3000 — homepage renders hero, features, CTA sections with Sanity content
2. Visit http://localhost:3000/contact — contact page renders with correct info
3. Visit http://localhost:3000/gallery — gallery images load from cdn.sanity.io
4. Visit http://localhost:3000/training — training page shows program cards/sections
5. Click into a training program — detail page shows rich text content (paragraphs, headings, lists)
6. Check browser console for errors on each page
  </action>
  <verify>
    <automated>echo "Manual visual verification required"</automated>
  </verify>
  <done>User confirmed all pages render correctly with Sanity content, no visual regressions, and Strapi env vars removed from Vercel.</done>
</task>

</tasks>

<verification>
- Full Playwright suite green (including training-programs.spec.ts)
- Zero Strapi/Blob references in codebase
- Build passes
- User confirms visual parity with current production site
</verification>

<success_criteria>
- All automated tests pass including rich text rendering tests
- All pages render migrated Sanity content correctly
- No broken images, missing text, or layout issues
- User approves visual verification
- Migration and cleanup are complete — Phase 6 done
</success_criteria>

<output>
After completion, create `.planning/phases/06-content-migration-and-cleanup/06-03-SUMMARY.md`
</output>
